-- local api = vim.api

-- local Finder = api.nvim_create_augroup("Finder", { clear = true })
-- api.nvim_clear_autocmds({ group = Finder })
-- api.nvim_create_autocmd("VimEnter", {
--   command = "if isdirectory(expand('%')) | exec 'Telescope file_browser cwd=' . expand('%') | endif",
--   group = Finder,
-- })
